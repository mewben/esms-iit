<div class="page-header">
	<h3><i class="fa fa-angle-right"></i> Assesment
		<small>Show student's Assesment.</small>
	</h3>
</div>

<div class="row">
	<div class="col-md-3">
		{{partial 'partials/lookup-studreg'}}
		<hr>
		{{#if studlist}}
			<div class="list-group">
				{{#each studlist}}
					{{#link-to 'assesments.assesment' studid class="list-group-item list-group-item-warning"}}
						<small>{{studid}} - {{studfullname}}</small>
					{{/link-to}}
				{{/each}}
			</div>
		{{/if}}
	</div>
	<div class="col-md-9">
		{{outlet}}
	</div>
</div>