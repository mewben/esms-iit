<form class="form-inline text-right" role="form" {{action 'lookup' on='submit'}}>
	<div class="form-group">
		<div class="input-group">
			{{input value=subj placeholder="Subject Code" class="form-control" autofocus="autofocus"}}
			<span class="input-group-btn">
				{{submit-spin saveText='Lookup' disabled=disbQSubj isProc=g.isProc}}
			</span>
		</div>
	</div>
</form>