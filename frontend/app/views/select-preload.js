/*import Ember from 'ember';

export default Ember.Select.extend({
	prompt: '- Select Bcode',
	classNames: 'form-control',

	didInsertElement: function() {
		this.get('controller').send('loadSelect', this.get('resource'), this.get('ctrl'));
	}
});*/